# =========================================================
# 라이브 트레이딩 설정
# =========================================================

from config.settings import INITIAL_CAPITAL, RISK_PER_TRADE

# =========================================================
# 전략 활성화 토글
# True: 해당 전략 사용, False: 비활성화
# =========================================================
LIVE_STRATEGIES = {
    'BREAKOUT': False,
    'MEAN_REV': True,
    'MOMENTUM': False,
}

# =========================================================
# 거래 심볼 및 레버리지
# =========================================================
LIVE_SYMBOL = "BTCUSDT"
LIVE_INTERVAL = "5m"
LIVE_LEVERAGE = 1  # 1x = 무레버리지

# =========================================================
# 리스크 관리
# =========================================================

# 최대 포지션 크기 (USDT)
# 한 전략당 최대 사용 가능 금액
MAX_POSITION_USDT = 100.0

# 일일 손실 한도 (%)
# 이 비율 이상 손실 시 당일 신규 진입 중단
DAILY_LOSS_LIMIT_PCT = 0.05  # 5%

# 최대 동시 포지션 수
MAX_CONCURRENT_POSITIONS = 2

# 전체 포트폴리오 손실 한도 (%)
# 이 비율 이상 손실 시 모든 거래 중단 (safe mode)
TOTAL_LOSS_LIMIT_PCT = 0.10  # 10%

# =========================================================
# 데이터 설정
# =========================================================

# 메모리에 유지할 캔들 수 (지표 계산에 필요)
CANDLE_BUFFER_SIZE = 500

# =========================================================
# 봇 운영 설정
# =========================================================

# 상태 저장 주기 (초)
STATE_SAVE_INTERVAL = 60

# 로그 레벨 ('DEBUG', 'INFO', 'WARNING', 'ERROR')
LOG_LEVEL = 'INFO'

# 콘솔 출력 활성화
CONSOLE_OUTPUT = True

# =========================================================
# 안전 설정
# =========================================================

# 주문 실행 전 확인 대기 시간 (초)
# 급격한 가격 변동 방지
ORDER_DELAY_SECONDS = 0.5

# API 요청 간 최소 간격 (초)
API_RATE_LIMIT_SECONDS = 0.1

# WebSocket 재연결 최대 시도 횟수
WS_MAX_RECONNECTS = 10

# 봇 시작 시 거래소 포지션 동기화
SYNC_ON_START = True

# =========================================================
# 알림 설정 (현재 비활성화)
# =========================================================
NOTIFICATIONS_ENABLED = False
TELEGRAM_BOT_TOKEN = ""
TELEGRAM_CHAT_ID = ""

# =========================================================
# 디버그 모드
# =========================================================

# True: 실제 주문 실행 안함 (시뮬레이션)
DRY_RUN_MODE = True

# True: 상세 로그 출력
VERBOSE_MODE = False
